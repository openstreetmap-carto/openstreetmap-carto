The salt pond pattern `symbols/salt_pond.svg` is generated from the source `salt_pond.svg` (a plain SVG, added in #4059) by

```
scripts/generate_landcover_patterns.py salt_pond
```

which sets the (white) pattern color and makes the file usable with Mapnik.
